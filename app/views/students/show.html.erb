<div class="container">
  <div class="row mt-5">
    <div class="col-4">
    </div>

    <div class="col-4">
      <p>Name: <%= @student.name %></p>
      <p>CNIC: <%= @student.cnic %></p>

      <%= link_to 'Edit', edit_student_path(@student), class: "btn btn-primary" %>
      <br>
      <%= link_to 'Show All Students', students_path, class: "btn btn-secondary mt-2" %>
    </div>

    <div class="col-4">
    </div>
  </div>
</div>
